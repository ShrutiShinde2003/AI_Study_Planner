import 'package:flutter/material.dart';

class GeminiAIPage extends StatelessWidget {
  const GeminiAIPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Gemini AI Page')),
      body: const Center(child: Text('This is the Gemini AI Page')),
    );
  }
}
